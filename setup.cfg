[pycalver]
current_version = "19.12.26.10"
version_pattern = "{yy}.{month}.{dom}.{PATCH}"
commit = True
tag = True
push = True

[pycalver:file_patterns]
setup.cfg =
    current_version = "{version}"

